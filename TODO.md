Let's not convert this python package to something we can also deploy in a centralized way alongside flowyml as a standalone product. So user can share notebooks, collaborate, create accounts, comment and review all notebooks with version controll and everythgin deepnote proposes in the payed version ! So that when we deploy both flowyml and flowyml notebook we have everything we need to mlops part and data epxloration working togeher simleasly but could entirely work as a separate proucts as well. In the sense that we configure in the notebook where is flowyml instance or use user local one -> to store experiemnts, productionnice, export to prod ready pipelines etc ! But it can also be able to function as a first local python package etc -> or maybe instead of complicating thigs about deployment we can desing it as an integration with a dedicated github repository that will centralize everything and will server as a DB so user can push, pull, comment etc and we have all the commands already from gh client etc !!! Maybe it's a better option than a cenralized instance with a db -> use github to handle collaboration, comments, versioning, branheds storage -> organize everything correctly per project user and everything (sam efor storage of reusable cells and everything)! I think this would be a killer feature that I wan't you to elaborate much further, local install + centralized github repository to collaboration teams interaction, versionning etc ... + integration with flowyml centralized instance if availble if not with local ones etc ...
But we need a good roganization of the github repository that will be handled automatically for users etc and good docs !

make sure notebooks are well saved and reloaded into dedicated projects, experiments, branches, etc maybe even autosaved etc

make sure we can move cells order down up etc and that play button is easy and in the reach for the user !!! Improve the UX and GUI !